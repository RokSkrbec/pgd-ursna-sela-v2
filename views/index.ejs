<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PGD Uršna sela</title>
  </head>
  <body>
    <h1>PGD Uršna sela</h1>
    <h1><%=notification.title %></h1>
    <h2><%=notification.content %></h2>
    <% posts.forEach(post => { %>
    <h2><%= post.title %></h2>
    <p><%= post.date %></p>
    <p><%= post.content %></p>
    <% }) %>
  </body>
</html>
